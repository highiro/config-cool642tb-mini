on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    # @main を、Zephyr 3.5 世代の安定したタグまたはブランチに変更する
    # v3.5 系の最終に近いコミットハッシュやブランチを指定します
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v3.5
